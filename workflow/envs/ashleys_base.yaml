name: ashleys_base
channels:
  - conda-forge
  - bioconda
dependencies:
  - samtools
  - tabix
  - bwa
  - sambamba
  - mosaicatcher
  # - alfred
  - ashleys-qc
  - pandas
  # PUBLISHDIR
  - rsync
  # MULTIQC
  - multiqc
  # Fix sklearn update
  - scikit-learn=1.2.2
  # Fix ashleys GH issue
  - python=3.10
